import { notFound } from 'next/navigation'; import { AppShell } from '@/app/components/app-shell'; import { getSet } from '@/app/lib/curriculum/load'; import { VocabularyRow } from '@/app/components/library/vocabulary-row';
export default async function SetPage({params}:{params:Promise<{setId:string}>}){const {setId}=await params; const set=getSet(setId); if(!set) notFound(); return <AppShell active="library"><section className="mx-auto max-w-3xl px-4 pb-28 pt-7"><a href="/library" className="text-sm text-cyan-300">← กลับคลังศัพท์</a><h1 className="mt-4 text-3xl font-bold text-white">{set.id} · {set.category}</h1><div className="mt-5 flex gap-3"><a href={`/learn/${set.id}`} className="inline-flex min-h-11 items-center rounded-xl bg-blue-500 px-4 font-semibold">เริ่มเรียน</a><a href={`/practice/${set.id}`} className="inline-flex min-h-11 items-center rounded-xl border border-white/15 px-4">ฝึกเกม</a></div><div className="mt-6 grid gap-2">{set.items.map(item=><VocabularyRow key={item.id} item={item}/>)}</div></section></AppShell>}

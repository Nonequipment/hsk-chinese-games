function tone(frequency:number,start:number,duration:number,gain=.045){const context=new AudioContext();const oscillator=context.createOscillator();const volume=context.createGain();oscillator.frequency.value=frequency;volume.gain.value=gain;oscillator.connect(volume).connect(context.destination);oscillator.start(context.currentTime+start);oscillator.stop(context.currentTime+start+duration);oscillator.onended=()=>context.close()}
export function playCorrect(){if(typeof AudioContext==='undefined')return;tone(520,0,.09);tone(700,.1,.12)} export function playWrong(){if(typeof AudioContext==='undefined')return;tone(180,0,.16,.035)}

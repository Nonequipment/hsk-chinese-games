import type { MemoryStatus } from '@/app/lib/progress/types';
export function MemoryControls({onChoose,disabled=false}:{onChoose:(status:MemoryStatus)=>void;disabled?:boolean}){return <div className="grid grid-cols-3 gap-2">{([['difficult','ยังยาก','border-rose-400/30 text-rose-600'],['learning','กำลังจำ','border-amber-400/30 text-amber-700'],['remembered','จำได้แล้ว','border-emerald-400/30 text-emerald-700']] as const).map(([status,label,color])=><button key={status} disabled={disabled} onClick={()=>onChoose(status)} className={`min-h-12 rounded-xl border bg-white px-2 text-sm font-semibold disabled:opacity-50 ${color}`}>{label}</button>)}</div>}

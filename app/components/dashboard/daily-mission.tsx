import { CalendarClock, ChevronRight, Sparkles } from 'lucide-react';
import type { DashboardSnapshot } from '@/app/lib/dashboard/service';
export function DailyMission({ snapshot }: { snapshot: DashboardSnapshot }) { return <section className="mission-grid relative overflow-hidden rounded-[1.75rem] border border-blue-300/20 bg-gradient-to-br from-blue-500/20 to-violet-600/10 p-6 sm:p-8"><div className="absolute right-5 top-5 grid size-11 place-items-center rounded-2xl bg-cyan-300/10 text-cyan-300"><Sparkles /></div><p className="text-sm font-semibold text-cyan-300">ภารกิจวันนี้</p><h2 className="mt-2 text-3xl font-bold text-white">แนะนำ {snapshot.recommendedSets} เซ็ต</h2><p className="mt-2 flex items-center gap-2 text-sm text-slate-300"><CalendarClock className="size-4" /> เหลือ {snapshot.daysRemaining} วันถึงเป้าหมาย</p><a href={`/learn/${snapshot.resumeSetId}`} className="mt-7 inline-flex min-h-12 items-center gap-2 rounded-2xl bg-white px-5 font-semibold text-slate-950 shadow-xl shadow-blue-950/30">เรียนต่อ <ChevronRight className="size-4" /></a></section>; }

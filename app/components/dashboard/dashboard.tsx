import type { DashboardSnapshot } from '@/app/lib/dashboard/service';
import { DailyMission } from './daily-mission'; import { ProgressRing } from './progress-ring';
export function Dashboard({ snapshot }: { snapshot: DashboardSnapshot }) { return <div className="grid gap-4 lg:grid-cols-[1.5fr_1fr]"><DailyMission snapshot={snapshot} /><section className="flex items-center justify-between rounded-[1.75rem] border border-blue-100 bg-white p-6 shadow-[0_12px_35px_rgba(30,64,175,.08)]"><div><p className="text-sm text-slate-500">ความก้าวหน้ารวม</p><p className="mt-2 text-2xl font-bold text-slate-950">{snapshot.completedSets} เซ็ตสำเร็จ</p><p className="mt-1 text-sm text-slate-500">เรียนแล้ว {snapshot.studiedWords} / 1,200 คำ</p></div><ProgressRing value={snapshot.completedSets} total={snapshot.totalSets} /></section><section className="grid grid-cols-3 gap-3 lg:col-span-2">{[['ผ่านแล้ว',snapshot.completedSets,'text-emerald-600'],['กำลังเรียน',snapshot.studyingSets,'text-blue-600'],['รอเริ่ม',snapshot.notStartedSets,'text-blue-400']].map(([label,value,color]) => <div key={String(label)} className="rounded-2xl border border-blue-100 bg-white p-4 shadow-sm"><p className="text-xs text-slate-500">{label}</p><p className={`mt-1 text-2xl font-bold ${color}`}>{value}</p></div>)}</section></div>; }

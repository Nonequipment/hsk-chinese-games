export function ProgressRing({ value, total }: { value: number; total: number }) { const percent = Math.round(value / total * 100); return <div aria-label={`ความก้าวหน้า ${value} จาก ${total} เซ็ต`} className="relative grid size-32 place-items-center rounded-full" style={{ background: `conic-gradient(#22d3ee ${percent}%,#17203b ${percent}% 100%)` }}><div className="grid size-[106px] place-items-center rounded-full bg-[#0a1024] text-center"><span className="text-2xl font-bold text-white">{percent}%</span><span className="-mt-5 text-xs text-slate-400">{value}/{total} เซ็ต</span></div></div>; }

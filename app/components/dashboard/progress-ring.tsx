export function ProgressRing({ value, total }: { value: number; total: number }) { const percent = Math.round(value / total * 100); return <div aria-label={`ความก้าวหน้า ${value} จาก ${total} เซ็ต`} className="relative grid size-32 place-items-center rounded-full shadow-inner" style={{ background: `conic-gradient(#2563eb ${percent}%,#dbeafe ${percent}% 100%)` }}><div className="grid size-[106px] place-items-center rounded-full bg-white text-center shadow-sm"><span className="text-2xl font-bold text-blue-800">{percent}%</span><span className="-mt-5 text-xs text-slate-500">{value}/{total} เซ็ต</span></div></div>; }

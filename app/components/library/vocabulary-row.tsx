import type { VocabularyItem } from '@/app/lib/curriculum/types';
export function VocabularyRow({ item }: { item: VocabularyItem }) { return <div className="grid grid-cols-[3rem_1fr_auto] items-center gap-3 rounded-2xl border border-blue-100 bg-white p-3 shadow-sm"><span className="text-xs text-slate-500">#{item.number}</span><div><p className="text-2xl font-semibold text-slate-950">{item.hanzi}</p><p className="text-sm text-blue-600">{item.pinyin}</p></div><p className="max-w-32 text-right text-sm text-slate-600">{item.thai}</p></div>; }

import type { VocabularySetRecord } from '@/app/lib/curriculum/types';
export function SetCard({ set }: { set: VocabularySetRecord }) { return <a href={`/library/${set.id}`} className="group block rounded-2xl border border-blue-100 bg-white p-4 shadow-sm transition hover:-translate-y-0.5 hover:border-blue-300 hover:bg-blue-50 hover:shadow-md"><div className="flex items-center justify-between"><span className="rounded-lg bg-blue-500/15 px-2.5 py-1 text-sm font-bold text-blue-600">{set.id}</span><span className="text-xs text-slate-500">20 คำ</span></div><h2 className="mt-3 line-clamp-2 min-h-12 font-semibold text-slate-950">{set.category}</h2><div className="mt-4 h-1.5 overflow-hidden rounded-full bg-blue-100"><div className="h-full w-0 rounded-full bg-cyan-400" /></div><p className="mt-2 text-xs text-slate-500">ยังไม่เริ่ม</p></a>; }

import { AppShell } from '@/app/components/app-shell';
import { chatGPTSignInPath, chatGPTSignOutPath, getChatGPTUser } from '@/app/chatgpt-auth';
export default async function ProfilePage() { const user = await getChatGPTUser(); return <AppShell active="profile"><section className="mx-auto max-w-3xl px-4 py-8"><p className="text-xs font-semibold uppercase tracking-[.22em] text-cyan-300">บัญชีผู้เรียน</p><h1 className="mt-2 text-3xl font-bold">โปรไฟล์</h1><div className="mt-6 rounded-3xl border border-white/10 bg-white/5 p-6">{user ? <><p className="text-lg font-semibold">{user.displayName}</p><p className="text-sm text-slate-400">{user.email}</p><a className="mt-5 inline-flex min-h-11 items-center rounded-xl border border-white/15 px-4" href={chatGPTSignOutPath('/')}>ออกจากระบบ</a></> : <><p className="text-slate-300">เข้าสู่ระบบเพื่อเก็บผลการเรียนไว้ทุกอุปกรณ์</p><a className="mt-5 inline-flex min-h-11 items-center rounded-xl bg-blue-500 px-4 font-semibold" href={chatGPTSignInPath('/profile')} target="_top">เข้าสู่ระบบด้วย ChatGPT</a></>}</div></section></AppShell>; }

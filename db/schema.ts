import { index, integer, primaryKey, sqliteTable, text, uniqueIndex } from "drizzle-orm/sqlite-core";
export const curriculumVersions = sqliteTable("curriculum_versions", { version: text().primaryKey(), itemCount: integer("item_count").notNull(), setCount: integer("set_count").notNull(), seededAt: text("seeded_at").notNull() });
export const vocabularySets = sqliteTable("vocabulary_sets", { id: text().primaryKey(), category: text().notNull(), scheduledDate: text("scheduled_date") });
export const vocabularyItems = sqliteTable("vocabulary_items", { id: text().primaryKey(), number: integer().notNull().unique(), setId: text("set_id").notNull(), category: text().notNull(), hanzi: text().notNull(), pinyin: text().notNull(), thai: text().notNull() }, t => [index("vocab_set_idx").on(t.setId)]);
export const userWordProgress = sqliteTable("user_word_progress", { userId: text("user_id").notNull(), itemId: text("item_id").notNull(), setId: text("set_id").notNull(), status: text().notNull(), studiedAt: text("studied_at").notNull(), updatedAt: text("updated_at").notNull() }, t => [primaryKey({ columns: [t.userId, t.itemId] }), index("word_user_set_idx").on(t.userId, t.setId), index("word_user_updated_idx").on(t.userId, t.updatedAt)]);
export const userSetProgress = sqliteTable("user_set_progress", { userId: text("user_id").notNull(), setId: text("set_id").notNull(), meaningPassedAt: text("meaning_passed_at"), hanziPassedAt: text("hanzi_passed_at"), tonesPassedAt: text("tones_passed_at"), completedAt: text("completed_at"), updatedAt: text("updated_at").notNull() }, t => [primaryKey({ columns: [t.userId, t.setId] })]);
export const examAttempts = sqliteTable("exam_attempts", { id: text().primaryKey(), userId: text("user_id").notNull(), setId: text("set_id").notNull(), stage: text().notNull(), seed: integer().notNull(), manifest: text().notNull(), score: integer(), passed: integer({ mode: "boolean" }), submittedAt: text("submitted_at"), createdAt: text("created_at").notNull() }, t => [index("exam_user_set_idx").on(t.userId, t.setId)]);
export const examAnswers = sqliteTable("exam_answers", { attemptId: text("attempt_id").notNull(), itemId: text("item_id").notNull(), answer: text().notNull(), correct: integer({ mode: "boolean" }).notNull() }, t => [primaryKey({ columns: [t.attemptId, t.itemId] })]);
export const gameAttempts = sqliteTable("game_attempts", { id: text().primaryKey(), userId: text("user_id").notNull(), setId: text("set_id").notNull(), mode: text().notNull(), score: integer().notNull(), missedItemIds: text("missed_item_ids").notNull(), createdAt: text("created_at").notNull() }, t => [index("game_user_created_idx").on(t.userId, t.createdAt)]);
export const userPreferences = sqliteTable("user_preferences", { userId: text("user_id").primaryKey(), autoSpeech: integer("auto_speech", { mode: "boolean" }).notNull().default(true), showPinyin: integer("show_pinyin", { mode: "boolean" }).notNull().default(true), effects: integer({ mode: "boolean" }).notNull().default(true), updatedAt: text("updated_at").notNull() });
export const mutationReceipts = sqliteTable("mutation_receipts", { userId: text("user_id").notNull(), requestId: text("request_id").notNull(), kind: text().notNull(), createdAt: text("created_at").notNull() }, t => [primaryKey({ columns: [t.userId, t.requestId] }), uniqueIndex("receipt_user_request_idx").on(t.userId, t.requestId)]);
